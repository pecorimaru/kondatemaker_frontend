import { createContext } from "react";
import { ToweekMenuPlanDetRowContextTypes } from '@/types';

export const ToweekMenuPlanDetRowContext = createContext<ToweekMenuPlanDetRowContextTypes | null>(null); 